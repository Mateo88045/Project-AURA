{
  "name": "chronos",
  "private": true,
  "version": "0.1.0",
  "description": "Chronos — Agentic scheduling app for high school students",
  "scripts": {
    "mobile": "pnpm --filter @chronos/mobile start",
    "mobile:ios": "pnpm --filter @chronos/mobile ios",
    "mobile:android": "pnpm --filter @chronos/mobile android",
    "typecheck": "tsc --noEmit",
    "clean": "pnpm -r exec rm -rf node_modules && rm -rf node_modules"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "pnpm@9.15.4"
}
